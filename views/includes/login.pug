.row.mt-5 
  .col-md-6.col-lg-5.m-auto
    .card.card-body
      h1.text-center.mb-3
        i.fas.fa-sign-in-alt
        span  Login
      include messages.pug        
      form(action="/users/login" method="POST")        
        .form-group
          input(
            type="email"
            id="email"
            name="email"
            class="form-control"
            placeholder="Enter Email"
          )
        .form-group
          input(
            type="password"
            id="password"
            name="password"
            class="form-control"
            placeholder="Enter Password"
          )
        button(type="submit" class="btn btn-primary btn-block") Login
      p.lead.mt-4 Don't have An Account? #[a(href="/users/register") Register]